# 发布说明 · 中文

每个版本的 GitHub Release 正文取本文件对应版本的小节（默认显示中文），
正文顶部有一个 **English** 链接，指向由 `CHANGELOG.md` 该版本小节生成的
`RELEASE-NOTES.en.md`（随 Release 附带）。

维护约定：每次发布在 `CHANGELOG.md` 写完英文条目后，为同一版本在这里补一段中文发布说明，
两个文件都要有对应版本的小节；每条改动末尾标注对应的 PR/issue 编号（#NN），
GitHub 会把 `#NN` 自动渲染成可点击的链接。

## 0.9.2

**可靠性：发不出去的卡会被重试，已被平台收下的卡不会发第二遍。**

在此之前，审批、提问或结果都只向平台投递一次：任何不是「体积超限」的失败，都会把审批退回桌面、或把结果记一条日志后丢弃。于是卡片即将发出的那一刻赶上网络抖动，走开的人就一张卡都收不到。现在这些投递都在同一个幂等键下重试——活动卡从 0.9.0 起就是这样做的。（本版改动直接提交到 main，见提交 45136f6，未走 PR/issue。）

### 变更

- **投递重试，每张卡同一个键。** 审批、提问或结果的投递失败会重试至多四次，每次间隔五秒，然后才放弃。每次尝试都带同一个 `uuid`，所以平台已收下但应答丢失的发送，会由它已经创建的那张卡应答——读者只看到一张卡，而不是两张。重试后仍发不出去，会被大声放弃：审批或提问退回桌面（桌面分支仍如以往权威），结果按原样丢弃。
- **结果的上家不再丢。** 更新的结果会先退休上一张卡，这在新的卡能落地时是对的；当新的永远没落地时，之前的通知现在会被放回——内存和持久存储都是——它的卡片也重新写回「活跃」，因此一次投递失败不会把读者最后的结果一起带走。
- **决定之后的改写会重试，而不是记一条日志就丢。** 卡片在已决定、已退休或已回答之后的重写，会重试至多三次，每次间隔一秒，然后才放弃。以前一次失败的重写会留下一张看起来仍可作答的卡——已决定请求上的按钮、已退休请求上的输入框——这是最糟的一种卡。

### 新增

- **飞书通道的存活探测。** 连接期间，通道按间隔向平台确认是否仍识别这个应用（与连接时检查相同的 tenant-token 调用）。探测发现凭据对被拒——应用被吊销或禁用——就关闭长连接并在设置卡上报告失败，`available()` 变为 false，审批不再推给一个无法应答的通道。平台不可达证明不了什么，保持原样、不来回翻页。间隔为 `channelConfig.probeIntervalMs`（默认 300 000 ms；`0` 关闭）。
- **过期的按下会被说出口。** 卡片上按下一个请求已不存在的按钮——重启之后，或请求已处理——现在会以 info 级别记录发生了什么，而不是只改写卡片并回一个 toast。

### 修复

- **结果投递幂等。** 平台已收下但应答丢失的结果，在体积重试时曾无键重发，可能变成两张卡。现在它与审批、提问卡一样带同一个 `uuid`。

## 0.9.1

**0.9.0 以来的三项修复，其中两项改变行为**：被 run「委派」工作的会话不再有自己的卡片；冻结卡片上的折叠不再带进它之前那一轮。两处都在下文单独说明——前者是排障文档早就承诺、代码却从未兑现的一条；后者移除了读者原本拥有的东西，因此都不该悄悄落地。

### 变更

- **折叠就是这一轮，之前的不算。** 活动卡的「本次执行过程」把读者刚回复过的那一轮也带上了，理由是卡片一变成这一轮的，它的正面就不再携带那个回答。但在真实会话里，那一轮占了折叠的 **72%**——是读者刚读过、刚答过的内容，却放在一个讲「本次执行过程」的标题下；两张卡对历史范围的说法也不一致（结果卡的折叠从来只含一轮），分隔两轮的只是 `---`，而模型自己的 markdown 里也有它。折叠现在只覆盖**一轮**：从读者发出的那句话，到它引发的一切结束为止，包括中间没人要求过的轮次（子代理结算、重试、排队的延续）。`user/message` 里没人写的那条也不再是边界，因此锚定一轮的那句话不会再被从它自己的折叠里丢掉。**你回复所依据的那个回答不再回到手机上**——它留在桌面，也留在结果卡里前一轮自己汇报的位置（决策 0027）。`run-record.js` 里那个死掉的「五轮读者」随之删除，这也让结果卡的「省略 N 字节」不再把整轮丢弃的内容算进去。（#65）

### 修复

- **一条指令不再变成几张卡。** 被 run 委派工作的会话曾被当作「人正在进行的对话」处理，于是扇出给子代理的任务给每个子代理都发了一张卡——而一张还没有消息的卡会被*发出*，每一张都会响。实测一次部署：一条指令的两个委派，在同一毫秒里产出了两条「创建」记录，然后各自结束轮次时又各来一张结果卡。两个产生方都在问提示词答不了的问题——委派的提示词以 `{ kind: 'user' }` 消息送达，和人发的同形——所以提问现在落回到会话自己的创建头，`origin` 是 harness 拒绝设为非 `"subagent"` 的字段。委派者没有活动卡、没有结果卡，也不会在手机易主时被接管；发起工作的那个会话仍照常汇报。**fork 不是委派**，保留它的卡（`delegated.js`，决策 0026）。排障文档早就把委派会话列入「什么都收不到」的原因——代码只是从未实现过它。（#63）
- **每张卡都点出它的会话，包括已经有会话的卡。** 那行小字来自 firehose 的 `session/title` 事件，而该事件只在会话第一次获得回退标题、生成器完成或有人重命名时追加——**对已存在的会话，以上都不会再发生**。于是每次重启后，既有会话的每张卡都悄悄少了这一行——一个「什么都没做」的特性：读者看到的和他们之前看到的一模一样。名字现在由卡第一次询问时从 harness 的标题服务读一次，并且只在 firehose 还没传来任何内容时读取——这样事件（包括重命名）仍然优先，没有该服务的部署行为如旧（`session-names.js`）。（#61）

## 0.9.0

**0.8.1 之后的第一个正式发布。** 树里曾编号过两个从未发布的版本——registry 的 `latest` 停在 0.8.1，而树的 package.json 写着 0.8.3——所以本节与下方 0.8.2 一节的改动，都是第一次到达读者。这是 minor 版本，因为它携带行为变化，每一条都在下文单独说明。

### 新增

- **每张卡都点出它的会话，而不只是项目。** 同一项目里的两个会话曾产生两张标题完全相同的卡，拿手机的人分不清。harness 给每个会话起的名字——从读者说的第一句话或生成的标题来——现在出现在头部的**副标题**：`DSH 结果 · my-app` 之上的 `会话：新会话任务与手机接管`。**标题本身不变**，读者已认识的东西都不动；字段以 `{ tag, content }` 接受、以裸字符串拒绝、不换行、`PATCH` 可移动，这正是晚到的名字能出现在活跃卡上的原因。还没有名字的会话渲染如旧，不留空行（`session-names.js`、`providers/feishu.js`，决策 0025）。（#56）
- **结果卡现在带上这一轮做了什么，而不只是最后一句话。** 以几次确认结尾的一轮会停在确认上，于是被确认的方案在手机上无处可寻，读者只能不看文本就决定下一步。这一轮——从读者最后说的那句话到它停下来——现在以同卡上的**折叠**形式带上，不额外花一条消息，这正是这张卡可接受的承诺。放不下时，按读者会选择的顺序整类丢弃，并注明字节数——工具行先合并为一条，再丢工具行，再丢读者自己的消息，最后才丢最早的叙述，因为最新输出才是决定所依据的（`results.js`，决策 0017）。（#29）
- **一轮做了什么按自己的账本记录，无论是否发过卡。** 结果卡只能显示模型的**最后**一条消息，而以几次确认结尾的一轮恰恰停在确认上——被确认的方案在手机上无处可寻。冻结卡折叠的记录曾挂在活动卡的账本上，而活动卡只在手机持有读者时存在，桌面上的 run 于是没有任何记录。现在每个会话从读者说的最后一句话起记录自己的一轮，与任何卡无关；被预算裁掉的内容计数而非静默丢失（`run-record.js`、`index.js`）。（#28）

### 变更

- **读者回答的那张卡，就是变化的那张卡。** 手机上的回复曾把「已收到指令」留在结果卡上，而它启动的 run 却出现在**另一条**消息——插件为手机接管的会话新造的活动卡。读者按下一张卡，却看着另一张卡动。回复所在的卡现在**变成该会话的卡**：run 显示在那里，run 自己的结果则是一张新卡，结果永远不会被焊进取代它的进度里。每轮消息数不变——会话第一轮两张，之后每轮一张（`activity.js`、`results.js`，决策 0021）。（#48）
- **会话忙时到达的回复进入那一轮。** 曾作为独立一轮排队，而在真实机器上排队的指令**丢了**：通知被消费、卡片变成 run、读者被告知已发出，而那一轮从未发生。运行中的会话现在把指令当作转向，在它的下一个步骤边界吸收——桌面输入框本来就是这么做的；空闲的会话仍得到独立一轮。到底是哪种，部署日志会点名，因为「我的话到了没有」否则无从回答（`results.js`，决策 0023）。（#52）
- **半途而止的一轮仍然给一张可作答的卡。** 此前结果到手机需两个条件同时成立：轮次以 `completed` 结束，且最后一条消息是不带工具调用的回答。出错或触顶的 run 都不满足——而且它们通常停在调用失败工具的那条消息上，根本没有文本。读者只剩一张刻意没有控件的活动卡，手机成了死胡同。`error` 和 `max-tokens` 现在都带一个回复框通知，卡片正面说明是哪一种，或 run 停下来前说了什么。读者已要求的停止（`aborted`）仍什么都不发（`results.js`，决策 0024）。（#54）
- **折叠只讲一轮，并标出读者说了什么。** 卡下的折叠曾是会话历史——最多五个已结束的轮次——一旦回复开始复用读者手里的卡，就会把读者在更早结果卡上已读过的内容再展示一遍；轮次边界画在 `turn/start` 上，它到达于读者消息*之后*，所以开启一轮的那句话被记到上一轮名下，会话的第一句话被整个丢掉，边界和读者自己的行也分不清。现在折叠是「回复所依据的那一轮 + 正在跑的那一轮」，边界在读者开口时画，记录在进入时构建，读者的行是唯一被标记（`**你**：`）的（`activity.js`、`results.js`，决策 0022）。（#51）
- **一张卡能装约七倍于从前的内容，因为当初设限的数字并不存在。** 所有长历史都被截断，原因是平台文档里抄来的 30 KB。实测本部署自己的应用：**131 KB 的卡体被接受**，164 KB 被拒——平台真正强制的是另外两个上限，同样实测得出：每卡 **200 个元素**（180 通过）和约 **51,000 个汉字**（12 万拉丁字符、2 万 emoji）。文本预算从 4.6 KB 提到 32 KB，新的元素预算 120，读者真正想读的 run 现在整段到达，而不是裁到五分之一（`budget.js`、`results.js`）。（#32）
- **装不下时，整类内容先于任何文本被放弃。** 旧规则两端保留、中间剪掉，凡方案长、回答也长时，读者奔着来的那部分恰恰被丢。现在折叠按读者会选的顺序退化：工具行合并为一个块（信息不变，一个元素代替多个），然后丢工具行，再丢读者自己的消息，最后才丢最早的叙述——因为最新输出才是读者正在决定的东西。丢什么都会注明字节数（`results.js`）。（#32）

### 修复

- **卡片状态可能被乱序读回。** 浏览器轮询部署状态时，慢响应可能落在新响应之后，卡片因此定格在一个已被取代的状态上。现在每个 tick 会退休它启动的请求，只应用最新的答案（`client.js`）。（#57）
- **桌面镜像三种静默方式。** 页面 composer 的拒绝被一律报告为「已处理」，真实失败被静默丢弃、桌面干等；轮询没有超时，挂掉的主机让镜像无声死掉；太老的主机每秒空转一次且不说话。拒绝现在按「composer 是否还在」分类（还在 → 为它保留决定；没了 → 视为已处理），轮询有截止时间，404 报告一次后停止（`client.js`）。（#57）
- **四个小假设的修正。** 被活动账本逐出的卡不再被认作这一轮的卡，过期按下不会再覆盖活跃的 run；被拒的剪贴板写入仍报告成功；失败的凭据采纳会清掉读者刚填好的表单；一个参数曾经没人传（`activity.js`、`client.js`、`escalation.js`）。（#57）
- **一轮曾显示一个它问不到的问题的答案。** 结束的轮次没有记为结束，于是下一轮的开始看起来还是同一轮：run 从未关闭，开启新轮的消息被孤儿化进一个没人读的 run——读者看到答案，问题却没了。轮次现在记录自己是打开的，一旦有一个打开，新的开始就关闭前一个 run。轮次*编号*和「run 里有没有内容」都无法区分这两者：号码会在日志被重置的会话里重复，人的消息总在声称它的轮次之前到达（`run-record.js`）。（#57）
- **折叠报告的省略字节数可能不准。** 曾按文本匹配已保留的条目，同一命令跑两次的 run 会把两次都算作保留——少报了读者缺失的内容，而这个数字存在的意义就是算准它。现在按位置匹配（`results.js`）。（#29）
- **手机接管后的第一个 run，恰是手机看不到的那个。** 人移步时正在跑的会话没有活动卡，卡片在会话的下一个事件时才懒造——而运行中的 run，下一个事件可能就是它的最后一个。于是移步后它做的第一件事，恰恰是有人拿起手机想看的那件事。移步到手机现在接管 registry 仍报告为运行中的每个会话（`activity.js`）。（#57）
